name: Discord Update Notification

on:
  push:
    branches: [main]

jobs:
  notify:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Bump version and send Discord notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          set -euo pipefail

          # ── Commit Info ──
          COMMIT_MSG=$(echo '${{ github.event.head_commit.message }}' | head -1)
          COMMIT_SHA="${{ github.sha }}"
          SHORT_SHA="${COMMIT_SHA:0:7}"
          AUTHOR="${{ github.event.head_commit.author.name }}"
          COMMIT_URL="${{ github.event.head_commit.url }}"
          REPO="${{ github.repository }}"
          TIMESTAMP="${{ github.event.head_commit.timestamp }}"
          PARENT_SHA=$(git rev-parse HEAD~1 2>/dev/null || echo "")

          # Skip auto version bump commits
          if echo "$COMMIT_MSG" | grep -q "^\[auto\] version bump"; then
            echo "⏭️ Skipping — version bump commit"
            exit 0
          fi

          # ══════════════════════════════════════════
          # SEQUENTIAL VERSION
          # ══════════════════════════════════════════
          if [ -f "VERSION" ]; then
            BUILD_NUM=$(cat VERSION | tr -d '[:space:]')
          else
            BUILD_NUM=335
          fi

          BUILD_NUM=$((BUILD_NUM + 1))
          VERSION="v6.${BUILD_NUM}"

          echo "$BUILD_NUM" > VERSION
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add VERSION
          git commit -m "[auto] version bump to ${VERSION}" || true
          git push || true

          # ══════════════════════════════════════════
          # ONLY LOOK AT source/*.lua
          # ══════════════════════════════════════════
          SOURCE_FILE=""
          for f in source/*.lua; do
            if [ -f "$f" ]; then
              SOURCE_FILE="$f"
              break
            fi
          done

          CHANGES=""
          STATS_LINE=""

          if [ -n "$SOURCE_FILE" ] && [ -n "$PARENT_SHA" ]; then
            if git diff --name-only "$PARENT_SHA" HEAD | grep -qF "$SOURCE_FILE"; then

              INS=$(git diff --numstat "$PARENT_SHA" HEAD -- "$SOURCE_FILE" | awk '{print $1}')
              DEL=$(git diff --numstat "$PARENT_SHA" HEAD -- "$SOURCE_FILE" | awk '{print $2}')
              STATS_LINE="📄 \`${SOURCE_FILE}\` — **+${INS:-0}** / **-${DEL:-0}** lines"

              ADDED_LINES=$(git diff "$PARENT_SHA" HEAD -- "$SOURCE_FILE" | grep "^+" | grep -v "^+++" || true)
              REMOVED_LINES=$(git diff "$PARENT_SHA" HEAD -- "$SOURCE_FILE" | grep "^-" | grep -v "^---" || true)

              # New Games
              NEW_GAMES=$(echo "$ADDED_LINES" | grep -oP '\{name\s*=\s*"\K[^"]+' 2>/dev/null | head -5 || true)
              if [ -n "$NEW_GAMES" ]; then
                GAME_LIST=""
                while IFS= read -r g; do
                  [ -n "$g" ] && GAME_LIST="${GAME_LIST}\`${g}\` "
                done <<< "$NEW_GAMES"
                [ -n "$GAME_LIST" ] && CHANGES="${CHANGES}🎮 **New Games:** ${GAME_LIST}\n"
              fi

              # New Functions
              NEW_FUNCS=$(echo "$ADDED_LINES" | grep -oP '(?:local\s+)?function\s+\K\w+' 2>/dev/null | sort -u | head -10 || true)
              if [ -n "$NEW_FUNCS" ]; then
                FUNC_LIST=""
                while IFS= read -r f; do
                  [ -n "$f" ] && FUNC_LIST="${FUNC_LIST}\`${f}\` "
                done <<< "$NEW_FUNCS"
                [ -n "$FUNC_LIST" ] && CHANGES="${CHANGES}🆕 **New Functions:** ${FUNC_LIST}\n"
              fi

              # Removed Functions
              REMOVED_FUNCS=$(echo "$REMOVED_LINES" | grep -oP '(?:local\s+)?function\s+\K\w+' 2>/dev/null | sort -u | head -10 || true)
              if [ -n "$REMOVED_FUNCS" ]; then
                ACTUALLY_REMOVED=""
                while IFS= read -r rf; do
                  if [ -n "$rf" ] && ! echo "$NEW_FUNCS" | grep -qxF "$rf"; then
                    ACTUALLY_REMOVED="${ACTUALLY_REMOVED}\`${rf}\` "
                  fi
                done <<< "$REMOVED_FUNCS"
                [ -n "$ACTUALLY_REMOVED" ] && CHANGES="${CHANGES}🗑️ **Removed Functions:** ${ACTUALLY_REMOVED}\n"
              fi

              # New Settings
              NEW_SETTINGS=$(echo "$ADDED_LINES" | grep -P '^\+\s{4,}\w+\s*=' 2>/dev/null | grep -oP '^\+\s+\K\w+' 2>/dev/null | sort -u | head -12 || true)
              if [ -n "$NEW_SETTINGS" ]; then
                SETTING_LIST=""
                COUNT=0
                while IFS= read -r s; do
                  if [ -n "$s" ] && [[ ! "$s" =~ ^(local|if|for|while|end|return|then|else|elseif|do|function|true|false|nil|Size|Position|Parent|Text|Name|BackgroundColor3|TextColor3|BorderSizePixel|Font|TextSize)$ ]]; then
                    SETTING_LIST="${SETTING_LIST}\`${s}\` "
                    COUNT=$((COUNT + 1))
                    [ $COUNT -ge 10 ] && break
                  fi
                done <<< "$NEW_SETTINGS"
                [ -n "$SETTING_LIST" ] && CHANGES="${CHANGES}⚙️ **New Settings:** ${SETTING_LIST}\n"
              fi

              # New Checkboxes
              NEW_CHECKBOXES=$(echo "$ADDED_LINES" | grep -oP 'createCheckbox\(\w+,\s*"\K[^"]+' 2>/dev/null | head -8 || true)
              if [ -n "$NEW_CHECKBOXES" ]; then
                CB_LIST=""
                while IFS= read -r cb; do
                  [ -n "$cb" ] && CB_LIST="${CB_LIST}• ${cb}\n"
                done <<< "$NEW_CHECKBOXES"
                [ -n "$CB_LIST" ] && CHANGES="${CHANGES}✅ **New Toggles:**\n${CB_LIST}"
              fi

              # New Sliders
              NEW_SLIDERS=$(echo "$ADDED_LINES" | grep -oP 'createSlider\(\w+,\s*"\K[^"]+' 2>/dev/null | head -6 || true)
              if [ -n "$NEW_SLIDERS" ]; then
                SL_LIST=""
                while IFS= read -r sl; do
                  [ -n "$sl" ] && SL_LIST="${SL_LIST}• ${sl}\n"
                done <<< "$NEW_SLIDERS"
                [ -n "$SL_LIST" ] && CHANGES="${CHANGES}🎚️ **New Sliders:**\n${SL_LIST}"
              fi

              # New Dropdowns
              NEW_DROPDOWNS=$(echo "$ADDED_LINES" | grep -oP 'createDropdown\(\w+,\s*"\K[^"]+' 2>/dev/null | head -4 || true)
              if [ -n "$NEW_DROPDOWNS" ]; then
                DD_LIST=""
                while IFS= read -r dd; do
                  [ -n "$dd" ] && DD_LIST="${DD_LIST}• ${dd}\n"
                done <<< "$NEW_DROPDOWNS"
                [ -n "$DD_LIST" ] && CHANGES="${CHANGES}📋 **New Dropdowns:**\n${DD_LIST}"
              fi

              # New Sections
              NEW_SECTIONS=$(echo "$ADDED_LINES" | grep -oP 'createSectionHeader\(\w+,\s*"\K[^"]+' 2>/dev/null | head -5 || true)
              if [ -n "$NEW_SECTIONS" ]; then
                SEC_LIST=""
                while IFS= read -r sec; do
                  [ -n "$sec" ] && SEC_LIST="${SEC_LIST}\`${sec}\` "
                done <<< "$NEW_SECTIONS"
                [ -n "$SEC_LIST" ] && CHANGES="${CHANGES}📂 **New Sections:** ${SEC_LIST}\n"
              fi

              # New Themes
              if echo "$ADDED_LINES" | grep -qP "Background\s*=\s*Color3"; then
                NEW_THEMES=$(echo "$ADDED_LINES" | grep -oP '^\+\s{4}\K\w+(?=\s*=\s*\{)' 2>/dev/null | head -5 || true)
                if [ -n "$NEW_THEMES" ]; then
                  THEME_LIST=""
                  while IFS= read -r t; do
                    [ -n "$t" ] && THEME_LIST="${THEME_LIST}\`${t}\` "
                  done <<< "$NEW_THEMES"
                  [ -n "$THEME_LIST" ] && CHANGES="${CHANGES}🎨 **New Themes:** ${THEME_LIST}\n"
                fi
              fi

              # WIP Flags
              NEW_WIP=$(echo "$ADDED_LINES" | grep -c 'wip\s*=\s*true' 2>/dev/null || echo "0")
              [ "$NEW_WIP" -gt 0 ] && CHANGES="${CHANGES}🚧 **WIP flags added:** ${NEW_WIP} game(s)\n"

              # External Scripts
              NEW_LOADSCRIPT=$(echo "$ADDED_LINES" | grep -oP 'loadScript\s*=\s*"\K[^"]+' 2>/dev/null | head -3 || true)
              if [ -n "$NEW_LOADSCRIPT" ]; then
                LS_LIST=""
                while IFS= read -r ls; do
                  [ -n "$ls" ] && LS_LIST="${LS_LIST}• \`${ls}\`\n"
                done <<< "$NEW_LOADSCRIPT"
                [ -n "$LS_LIST" ] && CHANGES="${CHANGES}📜 **New External Scripts:**\n${LS_LIST}"
              fi

              # Event Connections
              NEW_CONNS=$(echo "$ADDED_LINES" | grep -c ":Connect(function" 2>/dev/null || echo "0")
              [ "$NEW_CONNS" -gt 3 ] && CHANGES="${CHANGES}⚡ **New event hooks:** ${NEW_CONNS} connections\n"

            else
              STATS_LINE="📄 \`${SOURCE_FILE}\` — no changes"
            fi
          else
            STATS_LINE="⚠️ No source/*.lua file found"
          fi

          # ══════════════════════════════════════════
          # BUILD DESCRIPTION
          # ══════════════════════════════════════════
          DESC="**${COMMIT_MSG}**\n"

          [ -n "$STATS_LINE" ] && DESC="${DESC}\n${STATS_LINE}\n"
          [ -n "$CHANGES" ] && DESC="${DESC}\n${CHANGES}"

          if [ -z "$CHANGES" ] && [[ "$STATS_LINE" != *"no changes"* ]] && [[ "$STATS_LINE" != *"No source"* ]]; then
            DESC="${DESC}\n🔧 Minor update"
          fi

          if [ ${#DESC} -gt 3800 ]; then
            DESC="${DESC:0:3750}\n\n... and more"
          fi

          # ══════════════════════════════════════════
          # SEND TO DISCORD
          # ══════════════════════════════════════════
          PAYLOAD=$(cat <<EOF
          {
            "username": "Binxix Hub V6",
            "embeds": [{
              "title": "🚀 Binxix Hub — ${VERSION}",
              "url": "${COMMIT_URL}",
              "color": 10181558,
              "description": "${DESC}",
              "fields": [
                {
                  "name": "Version",
                  "value": "\`${VERSION}\`",
                  "inline": true
                },
                {
                  "name": "Commit",
                  "value": "[\`${SHORT_SHA}\`](${COMMIT_URL})",
                  "inline": true
                },
                {
                  "name": "Pushed by",
                  "value": "${AUTHOR}",
                  "inline": true
                }
              ],
              "footer": {
                "text": "Binxix Hub Updates • ${REPO}"
              },
              "timestamp": "${TIMESTAMP}"
            }]
          }
          EOF
          )

          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
               -H "Content-Type: application/json" \
               -d "$PAYLOAD" \
               "$DISCORD_WEBHOOK")

          if [ "$HTTP_CODE" = "204" ] || [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Discord notification sent!"
          else
            echo "⚠️ HTTP $HTTP_CODE — sending fallback"
            FALLBACK='{"username":"Binxix Hub V6","content":"🚀 **Update '"${VERSION}"'** — '"${COMMIT_MSG}"'\n🔗 '"${COMMIT_URL}"'"}'
            curl -s -H "Content-Type: application/json" -d "$FALLBACK" "$DISCORD_WEBHOOK"
            echo "✅ Fallback sent"
          fi
